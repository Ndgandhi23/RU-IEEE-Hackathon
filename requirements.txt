ultralytics==8.3.0
torch>=2.5
torchvision>=0.20
# Qwen3-VL requires transformers >= 4.57 (not yet on PyPI as of 2026-04).
# Install from git until the 4.57 wheel ships:
#   pip install git+https://github.com/huggingface/transformers@main
transformers @ git+https://github.com/huggingface/transformers@main
# transformers (git main) treats accelerate < 1.1.0 as "not installed" for device_map.
accelerate==1.13.0
bitsandbytes>=0.46.1
Pillow==10.4.0
opencv-python==4.10.0.84
numpy==1.26.4
pyyaml==6.0.2
requests==2.32.3
tqdm==4.66.5
pytest==8.3.3
# I/O plumbing
websockets==16.0             # brain/io/pi_bridge + pi/motor_controller server
fastapi==0.135.2             # brain/io/iphone_listener
uvicorn==0.42.0              # brain/io/iphone_listener entry point
